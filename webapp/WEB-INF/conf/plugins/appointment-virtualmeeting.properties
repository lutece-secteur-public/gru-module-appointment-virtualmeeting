# appointment-virtualmeeting properties file

#######################################################################################################
# Virtual Meeting Configuration

# Room empty timeout in seconds (room closes N seconds after last participant leaves)
virtualmeeting.room.emptyTimeout=0
